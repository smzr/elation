# Elation Project files

## Run locally

```
npm install
npm start
```
Then follow the instructions in the terminal

OR

```
npm install
npm run production
```
Then navigate to `./dist/index.html` to view the page